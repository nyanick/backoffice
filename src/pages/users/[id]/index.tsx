import {NextPage} from "next";

const UserDetails: NextPage = ({}) => {
    return (
        <section className='p-8'>
            <div>
                <h1 className='text-3xl font-medium'>User Details</h1>
            </div>
        </section>
    )
}

export default UserDetails
