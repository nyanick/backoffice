import {NextPage} from "next";

const PatientDetails: NextPage = ({}) => {
    return (
        <section className='p-8'>
            <div>
                <h1 className='text-3xl font-medium'>Patient Details</h1>
            </div>
        </section>
    )
}

export default PatientDetails
